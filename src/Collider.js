class ColliderRect extends Component {
    constructor( params ){    
        super("ColliderRect", entity);
        // todo parse params
    }
}

class ColliderCircle extends Component {
    constructor( params ){    
        super("ColliderCircle", entity);
        // todo parse params
    }
}